// -----------------------------------------------------------------------                             // -----------------------------------------------------------------------                          
public void testListArray() {                                                                          public void testListArray() {                                                                       
String input = "a  b c";                                                                               String input = "a  b c";                                                                            
StrTokenizer tok = new StrTokenizer(input);                                                            StrTokenizer tok = new StrTokenizer(input);                                                         
String[] array = tok.getTokenArray();                                                                  String[] array = tok.getTokenArray();                                                               
List list = tok.getTokenList();                                                                      | List&lt;?&gt; list = tok.getTokenList();                                                            
assertEquals(Arrays.asList(array), list);                                                              assertEquals(Arrays.asList(array), list);                                                           
assertEquals(3, list.size());                                                                          assertEquals(3, list.size());                                                                       
}                                                                                                      }                                                                                                   
