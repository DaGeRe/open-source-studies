private void checkContainsFiles(String prefix, File[] files, Collection results) {                   | private void checkContainsFiles(String prefix, File[] files, Collection&lt;File&gt; results) {      
for (int i = 0; i &lt; files.length; i++) {                                                            for (int i = 0; i &lt; files.length; i++) {                                                         
assertTrue(prefix + "[" + i + "] " + files[i], results.contains(files[i]));                            assertTrue(prefix + "[" + i + "] " + files[i], results.contains(files[i]));                         
}                                                                                                      }                                                                                                   
}                                                                                                      }                                                                                                   
