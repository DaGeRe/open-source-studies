public void testNextLineOnly() throws Exception {                                                      public void testNextLineOnly() throws Exception {                                                   
String encoding = null;                                                                                String encoding = null;                                                                             
File testFile = new File(getTestDirectory(), "LineIterator-nextOnly.txt");                             File testFile = new File(getTestDirectory(), "LineIterator-nextOnly.txt");                          
List lines = createFile(testFile, encoding, 3);                                                      | List&lt;String&gt; lines = createFile(testFile, encoding, 3);                                       
LineIterator iterator = FileUtils.lineIterator(testFile, encoding);                                    LineIterator iterator = FileUtils.lineIterator(testFile, encoding);                                 
try {                                                                                                  try {                                                                                               
for (int i = 0; i &lt; lines.size(); i++) {                                                            for (int i = 0; i &lt; lines.size(); i++) {                                                         
String line = iterator.nextLine();                                                                     String line = iterator.nextLine();                                                                  
assertEquals("nextLine() line " + i, lines.get(i), line);                                              assertEquals("nextLine() line " + i, lines.get(i), line);                                           
}                                                                                                      }                                                                                                   
assertFalse("No more expected", iterator.hasNext());                                                   assertFalse("No more expected", iterator.hasNext());                                                
} finally {                                                                                            } finally {                                                                                         
LineIterator.closeQuietly(iterator);                                                                   LineIterator.closeQuietly(iterator);                                                                
}                                                                                                      }                                                                                                   
}                                                                                                      }                                                                                                   
