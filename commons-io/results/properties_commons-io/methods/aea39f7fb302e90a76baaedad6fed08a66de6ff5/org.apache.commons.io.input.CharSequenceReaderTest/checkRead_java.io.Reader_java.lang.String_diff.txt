private void checkRead(Reader reader, String expected) throws IOException {                            private void checkRead(Reader reader, String expected) throws IOException {                         
for (int i = 0; i &lt; expected.length(); i++) {                                                       for (int i = 0; i &lt; expected.length(); i++) {                                                    
assertEquals("Read[" + i + "] of '" + expected + "'", (char) expected.charAt(i), (char) reader.read( | assertEquals("Read[" + i + "] of '" + expected + "'", expected.charAt(i), (char) reader.read());    
}                                                                                                      }                                                                                                   
}                                                                                                      }                                                                                                   
