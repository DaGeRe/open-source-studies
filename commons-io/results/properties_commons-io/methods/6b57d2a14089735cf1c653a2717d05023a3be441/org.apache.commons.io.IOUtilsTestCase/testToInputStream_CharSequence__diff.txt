@SuppressWarnings("javadoc")                                                                         |                                                                                                     
@Test                                                                                                  @Test                                                                                               
public void testToInputStream_CharSequence() throws Exception {                                        public void testToInputStream_CharSequence() throws Exception {                                     
final CharSequence csq = new StringBuilder("Abc123Xyz!");                                              final CharSequence csq = new StringBuilder("Abc123Xyz!");                                           
@SuppressWarnings("deprecation")                                                                     |                                                                                                     
InputStream inStream = IOUtils.toInputStream(csq);                                                     InputStream inStream = IOUtils.toInputStream(csq);                                                  
byte[] bytes = IOUtils.toByteArray(inStream);                                                          byte[] bytes = IOUtils.toByteArray(inStream);                                                       
assertEqualContent(csq.toString().getBytes(), bytes);                                                  assertEqualContent(csq.toString().getBytes(), bytes);                                               
inStream = IOUtils.toInputStream(csq, (String) null);                                                  inStream = IOUtils.toInputStream(csq, (String) null);                                               
bytes = IOUtils.toByteArray(inStream);                                                                 bytes = IOUtils.toByteArray(inStream);                                                              
assertEqualContent(csq.toString().getBytes(), bytes);                                                  assertEqualContent(csq.toString().getBytes(), bytes);                                               
inStream = IOUtils.toInputStream(csq, "UTF-8");                                                        inStream = IOUtils.toInputStream(csq, "UTF-8");                                                     
bytes = IOUtils.toByteArray(inStream);                                                                 bytes = IOUtils.toByteArray(inStream);                                                              
assertEqualContent(csq.toString().getBytes("UTF-8"), bytes);                                           assertEqualContent(csq.toString().getBytes("UTF-8"), bytes);                                        
}                                                                                                      }                                                                                                   
