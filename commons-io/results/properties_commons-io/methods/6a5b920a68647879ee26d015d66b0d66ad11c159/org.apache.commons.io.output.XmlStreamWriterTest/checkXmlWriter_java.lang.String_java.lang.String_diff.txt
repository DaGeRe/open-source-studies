private static void checkXmlWriter(String text, String encoding) throws IOException {                  private static void checkXmlWriter(String text, String encoding) throws IOException {               
String xml = createXmlContent(text, encoding);                                                       | checkXmlWriter(text, encoding, null);                                                               
String effectiveEncoding = (encoding == null) ? "UTF-8" : encoding;                                  |                                                                                                     
checkXmlContent(xml, effectiveEncoding);                                                             |                                                                                                     
}                                                                                                      }                                                                                                   
