public void initialize() throws Exception {                                                            public void initialize() throws Exception {                                                         
rootEntry.refresh();                                                                                 | rootEntry.refresh(rootEntry.getFile());                                                             
File[] files = listFiles(rootEntry.getFile());                                                         File[] files = listFiles(rootEntry.getFile());                                                      
FilesystemEntry[] children = files.length &gt; 0 ? new FilesystemEntry[files.length] : EMPTY_ENTRIES   FilesystemEntry[] children = files.length &gt; 0 ? new FilesystemEntry[files.length] : EMPTY_ENTRIES
for (int i = 0; i &lt; files.length; i++) {                                                            for (int i = 0; i &lt; files.length; i++) {                                                         
children[i] = createFileEntry(rootEntry, files[i]);                                                    children[i] = createFileEntry(rootEntry, files[i]);                                                 
}                                                                                                      }                                                                                                   
rootEntry.setChildren(children);                                                                       rootEntry.setChildren(children);                                                                    
}                                                                                                      }                                                                                                   
