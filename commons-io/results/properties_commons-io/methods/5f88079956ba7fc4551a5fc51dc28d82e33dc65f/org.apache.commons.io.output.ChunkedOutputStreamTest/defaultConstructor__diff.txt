@Test                                                                                                  @Test                                                                                               
public void defaultConstructor() throws IOException {                                                  public void defaultConstructor() throws IOException {                                               
final AtomicInteger numWrites = new AtomicInteger();                                                   final AtomicInteger numWrites = new AtomicInteger();                                                
ByteArrayOutputStream baos = getByteArrayOutputStream(numWrites);                                    | final ByteArrayOutputStream baos = getByteArrayOutputStream(numWrites);                             
ChunkedOutputStream chunked = new ChunkedOutputStream(baos);                                         | final ChunkedOutputStream chunked = new ChunkedOutputStream(baos);                                  
chunked.write(new byte[1024 * 4 + 1]);                                                                 chunked.write(new byte[1024 * 4 + 1]);                                                              
assertEquals(2, numWrites.get());                                                                      assertEquals(2, numWrites.get());                                                                   
chunked.close();                                                                                       chunked.close();                                                                                    
}                                                                                                      }                                                                                                   
